import React from 'react';
import styles from './Products.module.scss';

const Products = () => {
    return (
        <div className={styles.outerContainer}>
            <div className={styles.container}>Página de productos</div>
        </div>
    );
};

export default Products;
